# Result Template — Android Normalize Pinyin Route

Use this shape when handing the sprint back.

## Summary

- What changed:
- Files touched:
- Smoke status:
- What still needs validation:

## Build / Smoke

- Build host:
- Build command:
- Build result:
- Install target:
- Launch result:

## Runtime checks

- `GET /v1/pronunco/normalize-pinyin`:
- `POST /v1/pronunco/normalize-pinyin`:
- `GET/POST /v1/pronunco/compare-pinyin` regression:
- Any endpoint errors:

## Commit / Push

- Commit SHA:
- Push status:
- Anything intentionally left uncommitted:
